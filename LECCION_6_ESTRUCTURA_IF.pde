//Operadores de Comparación


//1. Igualdad (==): Compara si dos valores son iguales.
  // Ejemplo:
   int a = 5;
   int b = 7;
   boolean igual = (a == b); // igual es false

//2. Desigualdad (!=): Compara si dos valores no son iguales.
 //  Ejemplo:
   int x = 10;
   int y = 10;
   boolean desigual = (x != y); // desigual es false

//3. Mayor que (>): Compara si un valor es mayor que otro.
 //  Ejemplo:
   int m = 15;
   int n = 10;
   boolean mayor = (m > n); // mayor es true

//4. Menor que (<): Compara si un valor es menor que otro.
 //  Ejemplo:
   float p = 2.5;
   float q = 3.0;
   boolean menor = (p < q); // menor es true

//5. Mayor o igual que (>=): Compara si un valor es mayor o igual que otro.
 //  Ejemplo:
   int u = 8;
   int v = 8;
   boolean mayorOigual = (u >= v); // mayorOigual es true

//6. Menor o igual que (<=): Compara si un valor es menor o igual que otro.
 //  Ejemplo:
   double r = 4.0;
   double s = 4.5;
   boolean menorOigual = (r <= s); // menorOigual es true

//Estos operadores devuelven un valor booleano (true o false) según el resultado de la comparación. Puedes utilizarlos en condicionales, bucles y muchas otras situaciones en tus programas para tomar decisiones basadas en las relaciones entre valores.
